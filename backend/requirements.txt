# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.19

# --- Milestone 2: RAG stack (versions match notebook installs) ---
langchain-chroma==0.2.0
langchain-huggingface==0.1.2
langchain-community==0.3.13
langchain-core==0.3.28
langchain-text-splitters==0.3.4
sentence-transformers==3.3.1
chromadb==0.5.23
posthog<3.7.0  # chromadb 0.5.23's telemetry code calls posthog.capture() with a signature
               # that newer posthog releases (3.7+) changed, causing harmless but noisy
               # "Failed to send telemetry event ...: capture() takes 1 positional argument
               # but 3 were given" errors. Pinning avoids the mismatch. See chroma-core/chroma#4966.
google-genai==0.3.0
rouge-score==0.1.2
nltk==3.9.1

# --- Data ---
pandas==2.2.3
numpy==1.26.4
tabulate==0.9.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
