# Tests

Unit and API tests live in `backend/tests/` (run with `pytest` from `backend/`).
This top-level folder is reserved for future end-to-end / integration tests that
span both the backend and frontend (e.g. Selenium/Playwright smoke tests against
a running Streamlit + FastAPI stack).
